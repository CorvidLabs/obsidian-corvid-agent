schema_version = 1
profile = "standard"
[lifecycle]
command = ["fledge", "lanes", "run", "verify"]
[contract]
enabled = true
require_coverage = 0
skip_reason = "No prior numeric coverage threshold exists; production build and TypeScript checking remain blocking"
[risk]
threshold = "block"
[provenance]
mode = "soft"
policy = ".attest.json"
skip_reason = ""
[atlas]
enabled = false
skip_reason = "Standalone Atlas Pages publication remains independently managed"
